FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --omit=dev

COPY src/ ./src/

RUN mkdir -p /app/logs && chown -R node:node /app

USER node

# Workflow worker — executes NexusFlow automation workflows from workflow_queue
CMD ["node", "src/worker/workflow.worker.js"]
